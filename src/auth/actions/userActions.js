export const loginUser = (credentials, history, setFieldError, setSubmitting) => {
        // Fazer verificações e obter dados
        /*const user = {
            name: "George",
            "email": "gee@gmail.com"
        }*/
        const status = true;

        if (status === true) {
            //Permite acesso e redireciona
        }    else {
                // Retorna um erro ao usuário
            }
        }


export const signupUser = (credentials, history, setFieldError, setSubmitting) => {

}

export const logoutUser = () => {

}

